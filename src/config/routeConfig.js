const LAYOUT = '';

export const ROUTE_AUTH = {
    LOGIN : `${LAYOUT}/login`, 
    REGISTER : `${LAYOUT}/register`,  
}

export const ROUTE_USER = {
    HOME : `${LAYOUT}/`, 
    ABOUT : `${LAYOUT}/about`,  
    CONTACT : `${LAYOUT}/contact`,  
}